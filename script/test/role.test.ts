import { API } from "../lib/api";
import fetch from "node-fetch";
import { URL, URLSearchParams } from "url";
import { jwt, login } from "./login";

export async function createPermEntry(): Promise<void> {
}

